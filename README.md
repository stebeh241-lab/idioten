# Kolla ändrade filer, se aktiv branch, ...:
git status

# Lägg till filer inför commit
git add <fil>
git add --all

# Commit
git commit
git commit -m "<meddelande>"

# Pusha branch
git push origin <branch>

# Visa historik
git log --oneline
gitk --all&